# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Documentación interna de planificación (roadmap, notas) — no va al repo
docs/

# Config local de Claude Code — vive solo en la rama chore/claude-config,
# nunca en main/staging/feature (se añade ahí con `git add -f`)
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.iml
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
